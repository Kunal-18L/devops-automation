apiVersion: v1
kind: Service
metadata:
  name: java-maven-webapp-service
spec:
  selector:
    app: java-maven-webapp
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080

